<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fiek.unipr.mostwantedapp">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <application
        android:name=".helpers.SessionManagement"
        android:allowBackup="true"
        android:icon="@drawable/ic_app"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@drawable/ic_app"
        android:supportsRtl="true"
        android:theme="@style/Theme.MostWantedApp">
        <activity
            android:name=".auth.ForgotPasswordActivity"
            android:exported="false" />
        <activity
            android:name=".auth.PhoneSignInActivity"
            android:exported="false" />
        <activity
            android:name=".dashboard.InformerDashboardActivity"
            android:exported="false" />
        <activity
            android:name=".auth.GoogleSignInActivity"
            android:exported="false" />
        <activity
            android:name=".RegisterUserActivity"
            android:exported="false" />
        <activity
            android:name=".GetStartedActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBjrs7bTxymCa9op7ZsFyePGZpEKAA1pq8" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name=".maps.MapsActivity"
            android:exported="false"
            android:label="@string/title_activity_maps" />
        <activity android:name=".update.UpdateMultipleUsers" />
        <activity android:name=".lists.LocationActivity" />
        <activity
            android:name=".update.UpdatePerson"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".lists.UserActivity" />
        <activity android:name=".lists.PersonActivity" />
        <activity android:name=".helpers.PersonImage" />
        <activity android:name=".update.UpdateUser" />
        <activity android:name=".models.Profile" />
        <activity android:name=".dashboard.UserDashboardActivity" />
        <activity android:name=".dashboard.AdminDashboardActivity" />
        <activity android:name=".register.RegisterUser" />
        <activity android:name=".register.RegisterPerson" />
        <activity android:name=".LoginActivity" />
    </application>

</manifest>